# Core LLM / RAG
# Pinned to the 0.3.x line (not exact patches) so pip can resolve a mutually
# compatible set together with langchain-chroma's newer langsmith requirement.
langchain>=0.3.27,<0.4
langchain-ollama>=0.2.0,<0.3
langchain-community>=0.3.27,<0.4
langchain-chroma==0.2.6

# Embeddings (local, no external API key required)
langchain-huggingface>=0.1.2,<0.2
sentence-transformers==3.3.1

# Vector Store (client version must track the chromadb/chroma server image
# in docker-compose.yml - :latest currently serves API v1.0.0+)
chromadb==1.0.20

# Graph Database
neo4j==5.26.0          # Memgraph uses the Neo4j Bolt protocol

# API
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Data / Utils
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
tenacity==9.0.0

# Evaluation
ragas==0.2.6
datasets==3.1.0
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2

# Dev / Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Regulation Fetching
requests==2.32.3
beautifulsoup4==4.12.3
